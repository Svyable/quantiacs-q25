# Quantiacs API key (REQUIRED for local toolbox runs).
# Empty/blank does NOT work — qnt exits if API_KEY is ''.
# Create a free account and paste your profile key from:
#   https://quantiacs.com/personalpage/homepage
API_KEY=
