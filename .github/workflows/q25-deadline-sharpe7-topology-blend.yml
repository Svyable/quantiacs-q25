name: q25-deadline-sharpe7-topology-blend

on:
  pull_request:
    branches: [main]
    paths:
      - 'experiments/deadline_20260918_sharpe7_topology_blend/**'
      - 'research/measure_sharpe7_topology_blend.py'
      - '.github/workflows/q25-deadline-sharpe7-topology-blend.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  authoritative-multipass:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      API_KEY: default
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install -r requirements.txt
      - name: Verify frozen member source blobs
        run: |
          test "$(git rev-parse HEAD:strategies/generated/ebenezar_20260912_sharpe7_vol2.py)" = "cd2876aa22f81bad5ecc71637689e5cbe513c8f3"
          test "$(git rev-parse HEAD:strategies/generated/frontier_20260910b_topology_migration.py)" = "13aeb0ba3f35f284ced79cc62247d6d293a2a657"
      - name: Measure fixed equal-weight synthesis
        shell: bash
        run: |
          set -o pipefail
          python -m research.measure_sharpe7_topology_blend | tee sharpe7_topology_blend.txt
      - uses: actions/upload-artifact@v4
        with:
          name: q25-sharpe7-topology-blend-${{ github.run_id }}
          path: sharpe7_topology_blend.txt
          if-no-files-found: error
