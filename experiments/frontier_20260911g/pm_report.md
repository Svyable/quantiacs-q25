# Frontier-G — topology complements and signed response

Frontier-G starts after Frontier-D, E and F all froze. It does not retune their formulas and it does not retune topology-migration w84.

Implemented first-pass families:

1. `topology_rank_stability` — stable residual-network periphery rank while raw return leadership churns.
2. `topology_reconnection` — the opposite structural phase from migration: re-integration from prior periphery.
3. `response_sign_asymmetry` — positive-versus-negative common-shock assimilation asymmetry, gated by positive residual trend.

The first two ideas were reserved before later topology evidence was fully explored; signed-response asymmetry was preregistered before Frontier-C measurement. Exact implementations, grids and destructive controls in this commit are frozen before any Frontier-G market return is observed.

Three historically preregistered reserves remain unimplemented in this pass: bridge migration, eigen-concentration release and model-disagreement state. Their prior registrations remain the provenance source; Frontier-G does not silently rewrite them.

Selection remains development-only on 2016–2022 with the fixed 0/4/8/12% ATR-linked cost ladder. 2023+ cannot drive mutation.
