# Quantiacs toolbox — install ONE of:
#   conda: conda install -c quantiacs-source qnt
#   pip:   pip install "git+https://github.com/quantiacs/toolbox.git"
#
# Direct git dependency (pip):
qnt @ git+https://github.com/quantiacs/toolbox.git

# Factory helpers (optional — factory includes a minimal YAML fallback)
PyYAML>=6.0

# Typically pulled in by qnt; listed for clarity
numpy>=1.24
xarray>=2023.1
pandas>=2.0
