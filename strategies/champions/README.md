# Frozen champion mirrors

This directory preserves exact executable mirrors salvaged from the legacy `sota-top10-pages-20260907` branch before branch cleanup.

These files are **historical research artifacts**, not newly validated contest entries. Their executable bytes are preserved unchanged from the legacy branch blobs. Historical metrics and evidence boundaries remain governed by `configs/historical_top10.yaml` and `docs/STRATEGY_ATLAS.md`.

Included mirrors:

- `03_c165_mobility_rotation.py` — C165, source-mirror provenance retained in the module.
- `04_v12_volume_diffusion.py` — V12 signed cross-asset volume diffusion, source-mirror provenance retained in the module.

Do not attach newer metrics to these mirrors unless the exact file is rerun through the current Q25 research harness. Do not edit a frozen mirror in place; create a new candidate file for any modification.
